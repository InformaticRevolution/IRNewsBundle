{% trans_default_domain 'ir_news' %}

<div class="page-header">
    <h1>{{ 'admin.article.new.title'|trans }}</h1>
</div>

<form action="{{ path('ir_news_admin_article_new') }}" {{ form_enctype(form) }} method="POST" class="form-horizontal" novalidate>
    {% include 'IRNewsBundle:Admin/Article:form.html.twig' %}
    <div>
        <button type="submit" class="btn btn-primary">{{ 'admin.article.new.save'|trans }}</button>
    </div>         
</form>